'use client';
import * as TypeAnimationCore from 'react-type-animation';

const TypeAnimation = (props) => {
  return <TypeAnimationCore.TypeAnimation {...props} />;
};

export default TypeAnimation;
